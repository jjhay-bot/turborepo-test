{
  "name": "jak",
  "private": true,
  "packageManager": "pnpm@10.3.0",
  "scripts": {
    "preinstall": "node scripts/enforce-package-manager.cjs",
    "dev": "turbo run dev",
    "dev:admin": "pnpm --filter admin dev",
    "dev:portal": "pnpm --filter school-portal dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck"
  },
  "devDependencies": {
    "turbo": "^2.0.0"
  }
}
